torch
torchaudio
torchsde
# NOTE: stable-audio-tools is installed SEPARATELY by setup.sh with --no-deps,
# because its package metadata pins PyWavelets==1.4.1 (numpy 1.x ABI). That
# conflicts with our pywavelets>=1.6.0 requirement below and pip's resolver
# refuses to install both in one pass. setup.sh installs it after this file.
#
# Manually on a fresh shell:
#   pip install --no-deps git+https://github.com/Stability-AI/stable-audio-tools.git

# stable-audio-tools' transitive deps that --no-deps skipped during the
# install in setup.sh. Listed here so pip resolves them alongside the rest
# of the env and we don't hit ModuleNotFoundError mid-load.
alias-free-torch==0.0.6
dill
einops-exts
importlib-resources==5.12.0
nnAudio

# pytorch_lightning is imported by stable-audio-tools/.../lora/callbacks.py
# but not declared in their package metadata. Pull it ourselves.
pytorch-lightning
einops
transformers
accelerate
safetensors
soundfile
pyloudnorm
# pywavelets <1.6.0 was compiled against numpy 1.x and crashes at import
# under numpy 2.x with:
#   ValueError: numpy.dtype size changed, may indicate binary incompatibility.
# Pin both to versions that share the numpy 2 ABI.
numpy>=2.0
pywavelets>=1.6.0
pandas
tqdm
huggingface_hub

# Pitched-instrument pipeline (Phase 1.0+): quality gates + zone pre-rendering.
# Gate runs on GPU pod; enrich runs on local CPU.
librosa>=0.10
torchcrepe          # primary pitch detection, GPU-accelerated
basic-pitch         # polyphony detection (Spotify, also GPU-friendly)
pyrubberband        # pitch shift with formant preservation (calls rubberband-cli)
soxr                # high-quality resampling backend for librosa
# NOTE: pyrubberband shells out to `rubberband` (a.k.a. rubberband-cli).
# Install separately:
#   macOS:  brew install rubberband
#   ubuntu: apt-get install -y rubberband-cli
