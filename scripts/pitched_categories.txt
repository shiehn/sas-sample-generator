# Pitched-instrument categories for run_pitched.sh.
# Comment out (#) any line to skip that category for a run. Order is preserved.
# Every name here must have an entry in pitched_category_config.py and a
# prompts/pitched/<name>.txt.
#
# COST AWARENESS — v3 targets ~150 surviving instruments/category from ~200
# prompts. Generation = prompts × source-pitches × variants_per_prompt. With
# multi-source (SAS_MULTI_SOURCE=1) wide categories fan out 2-4 pitches, so the
# bottleneck is the (unbatched) GATE stage, not generation. Budget ~a day of
# big-GPU pod time for the full 28-category corpus; dial variant counts down in
# pitched_category_config.py if needed.
#
# `fx` is retired in v3 — sound-design one-shots (riser/impact/sweep/etc.) live
# in the drum/one-shot pipeline (see categories.txt).

# --- keys / synths / leads (EDM-forward) ---
synths
lead-supersaw
lead-fm
lead-acid
pluck-synth
plucks
keys
pianos
organs

# --- bass family ---
basses
808-bass
reese-bass

# --- pads / strings / brass / winds (sustaining) ---
pads
strings
brass
winds
accordion

# --- bells / mallets / tonal percussion ---
bells
mallets
percussion
timpani

# --- plucked / world / acoustic ---
guitars
banjos
mandolin
harp
sitar

# --- vocal ---
vocals
choir
